{
  "dependencies": {
    "elm": "^0.19.0",
    "elm-format": "^0.8.0",
    "elm-test": "^0.19.0-rev3",
    "elm-verify-examples": "^3.0.1",
    "elm-xref": "^0.3.2"
  },
  "scripts": {
    "test": "elm make && elm-xref && elm-format --validate src && elm-test && elm-verify-examples"
  }
}
